# By default, Jekyll ignores files or directories that start with a dot (.) or underscore (_)

exclude:
  - data/
  - templates/
  - tests/
  - build-character-guide-pages.ts
  - package-lock.json
  - package.json
  - tsconfig.json
